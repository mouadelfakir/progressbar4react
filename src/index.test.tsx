import { SimpleProgress, SmartProgress } from '.'

describe('SimpleProgress', () => {
  it('is truthy', () => {
    expect(SimpleProgress).toBeTruthy()
  })
});

describe('SmartProgress', () => {
  it('is truthy', () => {
    expect(SmartProgress).toBeTruthy()
  })
});
